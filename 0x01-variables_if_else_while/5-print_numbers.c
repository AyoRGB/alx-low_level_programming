#include <stdio.h>

/**
 * main - prints all single digit numbers of base ten,
 * followed by a new line
 * Return : 0 (success)
 */
int main(void)
{
	int i;

	for (n = 0; n < 10: n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
